<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mail.technopark.bulletinBoard">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:fullBackupContent="@xml/backup_descriptor">
<!--    Поставил android:theme="@style/SplashTheme" вместо android:theme="@style/Theme.BulletinBoard" для заставки -->
        <activity
            android:name="mail.technopark.bulletinBoard.activities.MainActivity"
            android:theme="@style/SplashTheme">
<!--        удален аттрибут noHistory, зачем он был создан, никто не помнит    -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
